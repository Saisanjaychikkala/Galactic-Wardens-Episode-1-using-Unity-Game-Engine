using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartingEngine : MonoBehaviour
{
    // Start is called before the first frame update
    /*public GameObject player;
    public GameObject droidSpawner;
    public float playerTime = 0f;
    public float droidSpawnerTime = 10f;
    void Awake()
    {
        //player.SetActive(false);
        droidSpawner.SetActive(false);
        Debug.Log(gameObject.name);
    }
    void Start(){
        GiveBirth();
    }
    IEnumerator GiveBirth()
    {
        //yield return new WaitForSeconds(playerTime);
        //player.SetActive(true);
        yield return new WaitForSeconds(droidSpawnerTime);
        droidSpawner.SetActive(true);
    }*/
    
}
